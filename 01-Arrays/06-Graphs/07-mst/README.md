## Minimum Spanning Tree (MST)

Goal:
Connect all nodes with minimum total edge weight.

Algorithms:
- Kruskal (uses DSU)
- Prim (uses Priority Queue)

When to use:
- Undirected weighted graph
- Want minimum total cost
